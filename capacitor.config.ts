import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.electrostat.cfg',
  appName: 'ElectroStat Config',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
